const checkNumbers = () => {
       const x = Number (prompt('Please enter the first number?'));

       for (let current = 0; current = 20; current ++) 
       if  (current %2 ==0) {
           console.log(current);   
       }
}
checkNumbers();